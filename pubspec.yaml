name: watch_connection_test
description: "Flutter project for watch connection test."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  watch_connectivity: ^0.1.6
  flutter_wear_os_connectivity: ^1.0.0
  flutter_watch_os_connectivity: ^1.0.0

  fluttertoast: ^8.2.5
  gap: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true